# ─── Database (Prisma Postgres) ────────────────────────────────────────────────
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DB?sslmode=require"

# ─── App ───────────────────────────────────────────────────────────────────────
APP_URL="http://localhost:3000"
# 32+ random chars. Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
AUTH_SECRET="dev_only_change_me_9f2c4a7b1e8d6350af12cc94be77d0e5"

# ─── Paystack ──────────────────────────────────────────────────────────────────
# Leave blank to run in MOCK mode (payments simulate instantly, no network calls).
PAYSTACK_SECRET_KEY=""
PAYSTACK_PUBLIC_KEY=""

# ─── Resend ────────────────────────────────────────────────────────────────────
# Leave blank to run in CONSOLE mode (emails logged to DB + terminal, nothing sent).
RESEND_API_KEY=""
EMAIL_FROM="Dominion House <camp@dominionhouse.org>"
